spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gestion_cinema_db
    username: postgres
    password: eqc116
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
  sql:
    init:
      data-locations: classpath:import.sql